apiVersion: v1
kind: Service
metadata:
  name: {{ include "devonberta-devops-challenge.fullname" . }}
  labels:
    {{- include "devonberta-devops-challenge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devonberta-devops-challenge.selectorLabels" . | nindent 4 }}
