apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devonberta-devops-challenge.fullname" . }}-test-connection"
  labels:
    {{- include "devonberta-devops-challenge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devonberta-devops-challenge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
